#include "Codexion.h"



int main(int ac, char **av)
{
    int (number_of_coders), (number_of_compiles_required), (time_to_burnout), (time_to_compile), (time_to_debug), (time_to_refactor), (dongle_cooldown);
    char *schedule;


arg_checker(av);

}
